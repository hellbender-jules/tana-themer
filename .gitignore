# Dependencies
node_modules/

# Build artifacts
dist/

# macOS
.DS_Store

# Logs / runtime
*.log
.injector.pid

# Editor / OS
.vscode/
.idea/
*.swp
Thumbs.db

# Code-signing / notarization secrets — never commit
*.p8
*.p12
*.pem
*.cer
notarization-*.json
notarization.log

# Code signature artifacts left behind by sign-and-notarize.sh
# (the .app committed to git is the unsigned source — signatures
# are generated locally per build and shouldn't be tracked)
Tana Themer.app/Contents/_CodeSignature/
Tana Themer.app/Contents/CodeResources
